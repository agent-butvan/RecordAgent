# 个人 Agent 工作台术语表

## 用户（User）

当前运行该本地优先工作台的单一所有者。第一期不提供远程多用户账户体系。

## 通知邮箱（Notification Email）

用户可选绑定的已验证收件地址，用于接收 Agent 提醒。它不是跨设备登录身份，也不承载云端密码找回。

## 日记录（Daily Event）

在某个日期发生、计划发生或被用户记录的一条生活上下文事件。它保留用户原始表达，并可附加类型、结构化字段、标签、附件、确认状态和来源。

日记录是日历、今日上下文及未来学习记录、健康记录等功能共享的核心时间索引。日历只是按月、按日聚合日记录的展示入口，不拥有待办、日程、花销或手记的专属数据。

所有类型只共享 ID、所有者、日期、类型、标题、来源、状态、版本和创建/更新时间等稳定字段；类型专属且需要查询、统计或约束的字段保存在独立详情结构中。新增类型不得向已有类型结构追加无关字段。

## 学习时段（Study Session）

用户从开始学习到结束学习之间的一段连续时间，是学习记录与学习时长统计的最小单位。学习时段包含学习内容、分类、开始时间、可选结束时间、时区和来源；未填写结束时间表示仍在进行中。

同一用户同一时刻最多存在一个进行中的学习时段。学习时长始终根据开始与结束时间计算，跨越自然日的时段在统计时按其时区拆分到对应日期，补卡只是一次性创建已结束学习时段的录入方式。

## 待确认草稿（Pending Draft）

Agent 从自然语言中提取、但尚未由用户确认的日记录。可被撤销；超过七天后降级为原始随记，不进入花销、计划或日程统计。

## 今日上下文（Today Context）

当前日期及近期摘要中，经过授权后可供 Agent 理解、规划、提醒和复盘的记录集合。

## 数据范围授权（Data Scope Consent）

用户对于某个模型、某类本地数据及其时间范围的发送许可。文本摘要可选择记住授权；图片、完整历史与财务记录始终逐次确认。

## 模型调用（Model Invocation）

一次发送给模型供应商并获得响应的真实 LLM 请求。一次对话轮次可能因为工具循环或子 Agent 包含多次模型调用。

## 对话轮次（Conversation Turn）

从一条用户消息开始，到对应 assistant 回复完成、失败或取消为止的连续交互，通过稳定的 `turnId` 关联。权限暂停和恢复仍属于原对话轮次。

## 轮次 Token 用量（Turn Token Usage）

同一对话轮次内全部模型调用由供应商上报的输入、输出与缓存输入 Token 聚合。缓存输入是输入 Token 的子集；供应商未返回的数据保持未知，不使用字符数推算。
